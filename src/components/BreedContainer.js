import React from 'react';
import styled from 'styled-components';

const BreedContainer = styled.span`
  display: block;
  flex: 1;
  margin-top: 8px;
  overflow-y: auto;
`;

export default BreedContainer;